{
  "name": "whisper-cloudflare",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm run copy-ffmpeg",
    "copy-ffmpeg": "node scripts/copy-ffmpeg.js",
    "deploy": "npm run copy-ffmpeg && wrangler deploy",
    "dev": "npm run copy-ffmpeg && wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@ffmpeg/ffmpeg": "^0.12.15"
  }
}
